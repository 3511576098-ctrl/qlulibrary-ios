name: Build iOS IPA

on:
  push:
    branches: [ main, master ]
  pull_request:
    branches: [ main, master ]
  workflow_dispatch:

jobs:
  build:
    name: Build Signed Ad-hoc IPA
    runs-on: macos-14

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Select Xcode
        run: |
          sudo xcode-select -s /Applications/Xcode_15.4.app || true
          xcodebuild -version

      - name: Build Xcode Project
        run: |
          xcodebuild clean build \
            -project qlulibrary.xcodeproj \
            -scheme qlulibrary \
            -configuration Release \
            -sdk iphoneos \
            -derivedDataPath build \
            CODE_SIGNING_ALLOWED=YES \
            CODE_SIGNING_REQUIRED=NO \
            CODE_SIGN_IDENTITY="-"

      - name: Ensure Ad-hoc Code Signature and Package IPA
        run: |
          APP_PATH="build/Build/Products/Release-iphoneos/qlulibrary.app"
          
          # Explicitly sign ad-hoc to produce valid _CodeSignature/CodeResources
          codesign --force --deep --sign - "$APP_PATH"
          codesign -dvvv "$APP_PATH"

          mkdir -p Payload
          cp -r "$APP_PATH" Payload/
          zip -r -y qlulibrary.ipa Payload
          ls -lh qlulibrary.ipa

      - name: Upload IPA Artifact
        uses: actions/upload-artifact@v4
        with:
          name: qlulibrary-signed-ipa
          path: qlulibrary.ipa
          retention-days: 30
